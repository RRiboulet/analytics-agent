# Keep the Docker build context lean and predictable.
# (This file lives in .devcontainer, so it also governs the
#  .devcontainer/ build context used by docker-compose.)
.git
.gitignore
node_modules
__pycache__
*.pyc
.venv
venv
env
.pytest_cache
.ruff_cache
build
dist
.mcp
.vscode
