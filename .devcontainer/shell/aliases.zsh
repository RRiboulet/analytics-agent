# Git
alias gs='git status'
alias gd='git diff'
alias gl='git log --oneline --decorate --graph -20'

# Docker
alias dc='docker compose'
alias dps='docker ps'

# Files
alias ll='ls -lah'
alias la='ls -A'

# Python
alias py='uv run python'

# API testing
# http :8000/health

# Navigation / dev quality-of-life
alias ..='cd ..'
alias ...='cd ../..'
alias c='clear'
alias reload-zsh='exec zsh'