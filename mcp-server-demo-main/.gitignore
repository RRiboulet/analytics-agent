.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.env
*.py[cod]

# Source data CSVs are intentionally committed so that `docker compose up`
# works on a fresh clone (db/init loads from data/olist). Exceptions:
!data/olist/
!data/olist/*.csv